name: Blue-Green Deploy to EC2

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      # 🔥 여기! application.yml 정확한 위치에 생성
      - name: Create application.yml
        run: |
          mkdir -p src/main/resources
          echo "${{ secrets.APPLICATION }}" > src/main/resources/application.yml

      - name: Print project structure
        run: |
          echo "===== CURRENT DIRECTORY ====="
          pwd
          echo "===== FILE TREE ====="
          ls -R .

      - name: SHOW application.yml INSIDE JAR
        run: |
          JAR_FILE=$(ls build/libs/*SNAPSHOT.jar | head -n 1)
          jar tf $JAR_FILE | grep application.yml
          echo "=== Extracting application.yml ==="
          jar xf $JAR_FILE BOOT-INF/classes/application.yml
          cat BOOT-INF/classes/application.yml

      - name: Build Spring Boot jar
        run: |
          chmod +x ./gradlew
          ./gradlew clean --no-build-cache build -x test

      - name: Verify built JAR
        run: |
          echo "📦 빌드된 JAR 확인 중..."
          ls -lh build/libs
          echo "🔍 PostgreSQL 드라이버 포함 여부:"
          jar tf build/libs/*SNAPSHOT.jar | grep postgresql || echo "❌ PostgreSQL 드라이버 없음"
          echo "🔍 application.yml 반영 여부:"
          jar tf build/libs/*SNAPSHOT.jar | grep application.yml || echo "❌ application.yml 누락됨"

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        run: |
          docker build --no-cache \
            -t ${{ secrets.DOCKER_USERNAME }}/stamppaw:latest \
            --platform linux/amd64 \
            -f Dockerfile .
          docker push ${{ secrets.DOCKER_USERNAME }}/stamppaw:latest

      - name: Deploy via SSH to EC2
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          envs: DOCKER_USERNAME,DB_URL,DB_USERNAME,DB_PASSWORD
          script: |
            cd ~/deploy
            chmod +x deploy.sh
            ./deploy.sh
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DB_URL: ${{ secrets.DB_URL }}
          DB_USERNAME: ${{ secrets.DB_USERNAME }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}